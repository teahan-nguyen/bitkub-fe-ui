export enum EButtonType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  WARNING = "warning",
  POSITIVE = "positive",
}

export enum ESize {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}

export enum EStatusBadge {
  ON_QUEUE = "On queue",
  CRAWLING = "Crawling",
  ERROR = "Error",
  ACTIVE = "Active",
  INACTIVE = "Inactive",
}
