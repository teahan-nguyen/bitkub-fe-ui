export enum EButtonType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  WARNING = "warning",
  POSITIVE = "positive",
}

export enum ESize {
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
}
