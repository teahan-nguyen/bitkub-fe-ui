import SetNewPassword from "@/components/auth/set-new-password";
import NewPasswordForm from "@/components/auth/set-new-password/components/new-password-form";
import * as React from "react";

export interface ISetNewPasswordPageProps {}

export default function SetNewPasswordPage(props: ISetNewPasswordPageProps) {
  return <SetNewPassword />;
}
